import React from 'react';
const Dashboard = () => {
  return (
    <section className="dashboard">
      <div className="heading-primary--main">Dashboard</div>
    </section>
  );
};

export default Dashboard;
